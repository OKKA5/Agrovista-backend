# Agrovista-backend

* copy `.env.example` to `.env`
 
* to seed admin run `node seed-admin.js`
  * make sure u change the uri in it to proper uri of ur db
* can find admin email and password in `.env`
